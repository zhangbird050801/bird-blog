<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.birdy.mapper.ArticleLikeMapper">

    <!-- 根据文章ID和用户ID查询点赞记录 -->
    <select id="selectByArticleIdAndUserId" resultType="com.birdy.domain.entity.ArticleLike">
        SELECT id, article_id, user_id, create_time, update_time, deleted
        FROM bg_article_like
        WHERE article_id = #{articleId}
          AND user_id = #{userId}
          AND deleted = 0
        LIMIT 1
    </select>

    <!-- 根据文章ID和用户ID删除点赞记录 -->
    <delete id="deleteByArticleIdAndUserId">
        DELETE FROM bg_article_like
        WHERE article_id = #{articleId}
          AND user_id = #{userId}
    </delete>

    <!-- 统计文章的点赞数量 -->
    <select id="countByArticleId" resultType="java.lang.Long">
        SELECT COUNT(*)
        FROM bg_article_like
        WHERE article_id = #{articleId}
          AND deleted = 0
    </select>

</mapper>