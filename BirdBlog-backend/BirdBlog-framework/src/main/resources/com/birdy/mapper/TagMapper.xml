<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.birdy.mapper.TagMapper">

    <resultMap id="BaseResultMap" type="com.birdy.domain.entity.Tag">
            <id property="id" column="id" />
            <result property="name" column="name" />
            <result property="remark" column="remark" />
            <result property="creator" column="creator" />
            <result property="createTime" column="create_time" />
            <result property="updater" column="updater" />
            <result property="updateTime" column="update_time" />
            <result property="deleted" column="deleted" />
    </resultMap>

    <resultMap id="TagVOResultMap" type="com.birdy.domain.vo.TagVO">
            <id property="id" column="id" />
            <result property="name" column="name" />
            <result property="remark" column="remark" />
            <result property="articleCount" column="article_count" />
    </resultMap>

    <sql id="Base_Column_List">
        id,name,remark,creator,create_time,updater,
        update_time,deleted
    </sql>

    <!-- 查询带文章数量的标签列表 -->
    <select id="selectTagsWithArticleCount" resultMap="TagVOResultMap">
        SELECT
            t.id,
            t.name,
            t.remark,
            COALESCE(article_count, 0) as article_count
        FROM bg_tag t
        LEFT JOIN (
            SELECT at.tag_id, COUNT(*) as article_count
            FROM bg_article_tag at
            INNER JOIN bg_article a ON at.article_id = a.id
            WHERE a.deleted = 0 AND a.status = 0
            GROUP BY at.tag_id
        ) ac ON t.id = ac.tag_id
        WHERE t.deleted = 0
        ORDER BY article_count DESC, t.id ASC
    </select>

</mapper>
